server:
  port: 8080

# 火山引擎 Ark 配置
ark:
  api-key: ${ARK_API_KEY:}  # 优先环境变量；也可直接写在此处
  bot-id: bot-20251005202857-74jzd  # 请替换为你的实际机器人ID

# Knife4j 文档配置（OpenAPI3）
knife4j:
  enable: true
  openapi:
    title: 脑筋急转弯api文档
    description: AI脑筋急转弯后端接口
    group:
      default:
        group-name: 默认分组
        api-rule: package
        api-rule-resources:
          - com.tfzhang.ainaojin.controller

